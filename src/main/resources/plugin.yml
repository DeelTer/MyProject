name: ${project.name}
version: ${project.version}
main: ru.deelter.myproject.${project.name}
api-version: 1.16

prefix: ${project.name}
authors: [ DeelTer ]
description: ${project.description}
website: ${project.url}

commands:
  test:
    aliases: [ testcommand ]
    description: simple command
